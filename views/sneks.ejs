<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Snek Pit</title>
    </head>
    <body>
        <h1>Welcome to the sSsnek Pit!</h1>
            <% for (idx in sneks) { %>
                <p>Name: <a href="/<%= sneks[idx]._id %>"><%= sneks[idx].name %></a></p>
                <p><a href="/<%= sneks[idx]._id %>/edit/">Edit snek</a></p>
                <form action="/<%= sneks[idx]._id %>/delete/" method='post'>
                    <input type="submit" value="Delete">
                </form>
                <hr>
           <% } %>
        <p><a href="/sneks/new">Create new snek</a></p>
    </body>
</html>